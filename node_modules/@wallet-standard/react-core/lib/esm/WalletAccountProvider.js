import React, { useState } from 'react';
import { WalletAccountContext } from './useWalletAccount.js';
/** TODO: docs */
export const WalletAccountProvider = ({ children }) => {
    const [walletAccount, setWalletAccount] = useState(null);
    return (React.createElement(WalletAccountContext.Provider, { value: {
            walletAccount,
            setWalletAccount,
        } }, children));
};
//# sourceMappingURL=WalletAccountProvider.js.map