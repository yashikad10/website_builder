import React, { useCallback, useMemo, useRef, useState } from 'react';
import { useWallet } from '../../useWallet.js';
import { SignTransactionContext } from './useSignTransaction.js';
/** TODO: docs */
export function hasSignTransactionFeature(features) {
    return 'experimental:signTransaction' in features;
}
/** TODO: docs */
export const SignTransactionProvider = ({ children, onError }) => {
    const { features } = useWallet();
    // Handle errors, logging them by default.
    const handleError = useCallback((error) => {
        (onError || console.error)(error);
        return error;
    }, [onError]);
    // Sign transactions with the wallet.
    const [waiting, setWaiting] = useState(false);
    const promise = useRef();
    const signTransaction = useMemo(() => hasSignTransactionFeature(features)
        ? async (...inputs) => {
            // If already waiting, wait for that promise to resolve.
            if (promise.current) {
                try {
                    await promise.current;
                }
                catch (error) {
                    // Error will already have been handled below.
                }
            }
            setWaiting(true);
            try {
                promise.current = features['experimental:signTransaction'].signTransaction(...inputs);
                return await promise.current;
            }
            catch (error) {
                throw handleError(error);
            }
            finally {
                setWaiting(false);
                promise.current = undefined;
            }
        }
        : undefined, [features, promise, handleError]);
    return (React.createElement(SignTransactionContext.Provider, { value: {
            waiting,
            signTransaction,
        } }, children));
};
//# sourceMappingURL=SignTransactionProvider.js.map