import React, { useCallback, useMemo, useRef, useState } from 'react';
import { useWallet } from '../../useWallet.js';
import { SignAndSendTransactionContext } from './useSignAndSendTransaction.js';
/** TODO: docs */
export function hasSignAndSendTransactionFeature(features) {
    return 'experimental:signAndSendTransaction' in features;
}
/** TODO: docs */
export const SignAndSendTransactionProvider = ({ children, onError }) => {
    const { features } = useWallet();
    // Handle errors, logging them by default.
    const handleError = useCallback((error) => {
        (onError || console.error)(error);
        return error;
    }, [onError]);
    // Sign transactions with the wallet.
    const [waiting, setWaiting] = useState(false);
    const promise = useRef();
    const signAndSendTransaction = useMemo(() => hasSignAndSendTransactionFeature(features)
        ? async (...inputs) => {
            // If already waiting, wait for that promise to resolve.
            if (promise.current) {
                try {
                    await promise.current;
                }
                catch (error) {
                    // Error will already have been handled below.
                }
            }
            setWaiting(true);
            try {
                promise.current = features['experimental:signAndSendTransaction'].signAndSendTransaction(...inputs);
                return await promise.current;
            }
            catch (error) {
                throw handleError(error);
            }
            finally {
                setWaiting(false);
                promise.current = undefined;
            }
        }
        : undefined, [features, promise, handleError]);
    return (React.createElement(SignAndSendTransactionContext.Provider, { value: {
            waiting,
            signAndSendTransaction,
        } }, children));
};
//# sourceMappingURL=SignAndSendTransactionProvider.js.map